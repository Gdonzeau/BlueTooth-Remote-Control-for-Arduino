//
//  Button.swift
//  BlueTooth Remote Control for Arduino
//
//  Created by Guillaume Donzeau on 13/12/2021.
//

import UIKit
/*
class Button: UIView {
    var button = UIButton()
    var name = UILabel()
    var order: String = ""
    
    
    
    override init(frame: CGRect) {
        super.init(frame: frame)
        self.translatesAutoresizingMaskIntoConstraints = false
        setupView()
    }
    
    required init?(coder: NSCoder) {
        super.init(coder:coder)
    }
    
    func setupView() {
        button.setTitle("Bouton 02", for: .normal)
        name.text = "Bouton 01"
        backgroundColor = UIColor(displayP3Red: 0.2, green: 0.2, blue: 0.2, alpha: 0.5)
        name.textColor = .label
        name.font = UIFont.preferredFont(forTextStyle: .caption1)
        name.adjustsFontForContentSizeCategory = true
        name.translatesAutoresizingMaskIntoConstraints = false
       
        let contentStackView = UIStackView(arrangedSubviews: [button])
        contentStackView.axis = .horizontal
        contentStackView.alignment = .fill
        contentStackView.distribution = .fill
        contentStackView.spacing = 5
        contentStackView.translatesAutoresizingMaskIntoConstraints = false
        addSubview(contentStackView)
        
        NSLayoutConstraint.activate([
            contentStackView.leadingAnchor.constraint(equalTo: leadingAnchor),
            contentStackView.trailingAnchor.constraint(equalTo: trailingAnchor),
            contentStackView.topAnchor.constraint(equalTo: topAnchor),
            contentStackView.bottomAnchor.constraint(equalTo: bottomAnchor),
        ])
    }
}
 */
