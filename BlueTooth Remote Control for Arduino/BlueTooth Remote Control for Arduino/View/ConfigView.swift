//
//  NineButtonsSquareConfig.swift
//  BlueTooth Remote Control for Arduino
//
//  Created by Guillaume Donzeau on 17/12/2021.
//

import UIKit
/*
class ConfigView: UIStackView {
    var appColors = AppColors.shared
    var infoButtons = InfoButtons()
    
    let pageHead = OneTextFieldLine()
    let rankButtons01 = ThreeButtonsLine()
    let rankButtons02 = ThreeButtonsLine()
    let rankButtons03 = ThreeButtonsLine()
    
    let rankOrders01 = ThreeTextFieldsLine()
    let rankOrders02 = ThreeTextFieldsLine()
    let rankOrders03 = ThreeTextFieldsLine()
    
    let nameButtons01 = ThreeTextFieldsLine()
    let nameButtons02 = ThreeTextFieldsLine()
    let nameButtons03 = ThreeTextFieldsLine()

    //let connection = Connection()
    let datas = TwoTextFieldsLine()
    
    override init(frame: CGRect) {
        super.init(frame: frame)
        
        setupView()
    }
    
    required init(coder: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }
    
    func setupView() {
        backgroundColor = appColors.backgroundColor
        
        alignment = .fill
        axis = .vertical
        distribution = .fill
        translatesAutoresizingMaskIntoConstraints = false
        spacing = 5
        
        addArrangedSubview(pageHead)
        addArrangedSubview(rankButtons01)
        addArrangedSubview(rankOrders01)
        addArrangedSubview(nameButtons01)
        addArrangedSubview(rankButtons02)
        addArrangedSubview(rankOrders02)
        addArrangedSubview(nameButtons02)
        addArrangedSubview(rankButtons03)
        addArrangedSubview(rankOrders03)
        addArrangedSubview(nameButtons03)
        addArrangedSubview(datas)
        
    }
}
*/
